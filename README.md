# StudentIT Calendar Parser (Rewrite)

## Usage
Set your python path to the root directory of this project. Easiest way to do this is something like this:

```
cd calendar
export PYTHONPATH=$PYTHONPATH:`pwd`
```
